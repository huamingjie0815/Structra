# Local Verification Log

Date: 2026-06-08

Scope: local single-user desktop verification for the current Structra implementation.

Current architecture marker: `src/App.tsx` is 1258 lines after extracting connector presets, page actions, inline label editing, comment workflow, element editing, clipboard, shape insertion, canvas-settings, command-items, command-palette, editor keyboard, presentation, document history controller, local document persistence, native menu, native menu enabled-state modeling, desktop window lifecycle workflows, keyboard connector logic, non-history focus selection workflow, local workspace action workflow, and workspace/editor URL route state, plus adding a standalone local workspace shell, local document identity display, local preferences dialog, export panel, a persistent editor topbar return-to-workspace action, native File menu return-to-workspace command, local template favorites, and audit-only native commands for restarted Recent reopen plus unsaved-prompt evidence. This number is a complexity signal, not a release gate; the next acceptance decision should be based on responsibility locality, command/history consistency, and regression evidence rather than a fixed 1200-line ceiling. Inline label commits, element-editing graph mutations, structured Mind/Org child/sibling insertion, clipboard paste/cut/duplicate/delete, shape insertion, pointer connect/reconnect, keyboard connector creation, React Flow drag/resize commits, edge waypoint/bend commits, comment add/reply/resolve/delete, whole-document New/Open/legacy import/version restore baseline replacement, page navigation baseline replacement, Mermaid import, version restore, document-level canvas settings, and Mind/Org layout commands now flow through explicit command/controller paths. Non-history focus selection for outline, document-search node targeting, select-all, and comment targeting now flows through `src/editor/focusSelection.ts` and `src/features/editor/useFocusSelectionWorkflow.ts`. Canvas viewport state now syncs on move end instead of every move frame, which keeps the 1000-node pan baseline inside the current guardrail. The remaining architecture targets are panel composition and desktop automation locality; the remaining product targets are professional diagram-family depth, import/export visual trust, large-diagram usability, Finder/window-title evidence, and export pixel/visual baselines.

## Local Product Fit Pass

This pass continued feature completion before performance work. The local review panel no longer uses collaboration-oriented module naming or user-visible wording: comments and snapshots are presented as local review comments and local versions. The document format model now consistently treats `.structra` as native local document extensions across workspace display, file input accept lists, native open/save filters, document name normalization, format-support UI, Tauri read/write extension validation, unit fixtures, and browser file chooser import. Mermaid import now fails with explicit parse errors for unsupported diagram families, malformed flowchart syntax, and empty effective content. SVG export now escapes imported styling/text attribute boundaries before writing XML.

## Documentation Audit

This pass reviewed the product docs and current code shape for two risks: whether previous wording still implied a fixed `App.tsx <1200 lines` requirement, and which local single-user ProcessOn-like product gaps remain after the latest architecture work. The roadmap and checklist now describe `App.tsx` size as a review marker only. Product readiness is now framed around clear module responsibilities, predictable document transactions, native desktop lifecycle evidence, realistic editor workflows, diagram-family depth, import/export trust boundaries, accessibility, and large-diagram usability.

## Functional File-Lifecycle Pass

This pass focused on local single-user product functionality, not performance. `src/features/document/documentFileLifecycle.ts` now owns the pure state transitions for native save success, browser-download fallback, active-path write failure, new untitled document state, browser-imported JSON state, missing recent-file removal, and unsaved-change discard confirmation. `useDocumentFileWorkflow` applies those patches instead of spreading file-lifecycle state updates through the hook. New documents now remain dirty local drafts until saved, and destructive JSON/.structra/Mermaid imports now confirm before replacing current work, matching desktop document expectations.

## Diagram-Family Authoring Pass

Mind map and org chart templates now have direct structure-growth commands instead of only generic shape insertion plus auto-layout. `src/editor/elementOperations.ts` can append child or sibling nodes for Mind/Org families with deterministic node data, handles, edges, selection, and undoable graph transactions. The toolbar, command palette, `Tab` child shortcut, and `Enter` sibling shortcut expose these as local authoring commands, while unit and Playwright smoke coverage assert Mind child, Mind sibling, Org child, keyboard child, and keyboard sibling creation plus undo behavior.

The local workspace also now supports clearing the recent-document list without touching any document file. Playwright smoke seeds local recent entries, verifies native/compatible format badges, clicks a browser-preview recent document and confirms the native recent entries are preserved with a desktop-required error, confirms the failed open stays in the workspace instead of switching to editor mode, clears the list through the UI, and asserts the local recent-document storage key is removed.

## Workspace Navigation Pass

The local workspace and editor now have explicit URL-backed desktop flow state. `src/domain/workspaceRoute.ts` treats `?id=<local-session>` as editor mode and `?workspace=1` as an explicit workspace return state; no parameter still falls back to the user's startup workspace preference. Entering the editor, choosing a blank workspace document, or applying a workspace template writes editor mode, while the persistent editor topbar return button and native File menu workspace command write workspace mode. Browser smoke now verifies that refreshing after entering the editor or choosing a blank workspace document stays in the editor instead of reopening the workspace, and that the visible return button can reopen the workspace. Desktop smoke verifies the packaged native menu exposes `open-workspace=true` and that a real macOS File menu click reaches the frontend with `handled=true`.

The workspace is now rendered as a standalone shell instead of an overlay on top of the editor. Browser smoke asserts that the workspace has no editor topbar, no command toolbar, and no canvas region behind it; entering the editor re-renders those editor-only regions, and returning to the workspace hides them again. The same pass fixed the workspace-layer import-error banner so failed local-path opens can still be dismissed while the workspace is active.

Desktop evidence for the workspace Recent entry workflow now emits Tauri-gated `workspace-recent-open-attempt` and `workspace-recent-open-result` audit records around the same `openRecentDocument` path used by the UI. The native menu audit dispatcher has an audit-only `open-first-recent-from-workspace` command to exercise that workflow without relying on unstable WebView AX clicks. Unsaved destructive document actions emit `unsaved-discard-prompt` with action, allowed, and source. The current `pnpm qa:desktop` run verified both paths, including an audit-only one-shot prompt override that proves the dirty-document guard fired without blocking on WebView `window.confirm`.

## Current Completion Batch

This batch added product-facing local desktop polish before the final verification pass. The workspace no longer renders the editor toolbar/canvas behind it, editor/workspace URL mode uses `?id=<local-session>` and `?workspace=1`, the editor has a visible return-to-workspace button, preferences now include light/dark/system theme, default export format is surfaced through an export panel, the command palette includes all shape insertion commands plus default/document PDF/print export commands, selected nodes can be matched by width/height/both size, local custom templates can be deleted from workspace and sidebar entry points, page delete/template replacement/version restore/custom-template delete/recent-list clear are confirmation-protected, and canvas setting changes now route through the document history controller.

Fresh verification has now run after this batch. `pnpm test`, `pnpm build`, `cargo test --manifest-path src-tauri/Cargo.toml`, `pnpm test:e2e`, `pnpm tauri:build`, `pnpm qa:desktop`, `node scripts/qa/accessibility-keyboard-audit.mjs`, and `git diff --check` passed on 2026-06-08. Performance remains intentionally deferred until the user explicitly asks for the performance pass.

## Template Favorites Completion Batch

This batch adds local template favorites as the next product-facing workspace/library capability before verification closeout. Template favorite ids persist locally under `structra-template-favorites-v1`, are capped and normalized, appear in both the standalone local workspace and editor sidebar, expose a workspace "收藏" category, contribute to workspace asset counts, and are removed when a deleted local custom template had been favorited. This remains a local single-user asset feature only: there is still no account, sync, cloud template marketplace, or sharing requirement in scope.

Fresh verification for this batch passed on 2026-06-08. `pnpm test`, `pnpm build`, `pnpm test:e2e`, `node scripts/qa/accessibility-keyboard-audit.mjs`, and `git diff --check` passed. The updated Playwright smoke now clicks the workspace favorite action, verifies the workspace "收藏" filter, verifies favorite persistence across reload, then toggles the same template from the editor sidebar. Performance remains intentionally deferred.

## Desktop File Association Pass

The Tauri bundle now declares `.structra` as editable Structra local documents through `bundle.fileAssociations`, including a local MIME type and macOS exported document type. Fast unit coverage asserts that the packaged desktop configuration keeps both native extensions associated with the app and does not add external network URLs to that association. This is necessary desktop lifecycle infrastructure, but it is not a substitute for direct Recent reopen, Finder/window-title, and unsaved-prompt evidence.

## Desktop External Open Pass

The packaged desktop app now has a runtime bridge for files opened by the OS after association. The Rust entry point filters startup arguments and Tauri `RunEvent::Opened` file URLs down to absolute `.structra`, `.structra`, or `.json` document paths, caches pending paths, and emits a `native-open-documents` event. The React side registers a desktop open-document workflow, drains pending paths on startup, deduplicates repeated events, opens the first usable document through the same native path import lifecycle as Recent/Open, and closes the workspace into editor mode after a successful external open. Successful native Open, Recent reopen, Save, Save As, and export paths now emit desktop lifecycle audit events through the same Tauri-gated bridge; current desktop automation uses this for real Open, Save As, Export JSON, Export SVG, Export PNG, current-page Export PDF, and document Export PDF picker paths. Rust unit coverage asserts argument and file-URL filtering; JS unit coverage asserts frontend payload normalization, deduplication, and the desktop lifecycle audit bridge for open/save/export hooks.

`pnpm qa:desktop` now registers the packaged app with LaunchServices, launches the packaged app with desktop lifecycle audit enabled, verifies that the Tauri native menu state has synchronized the initial `document-save=false` disabled state, and verifies that native menu commands reach the frontend command guard: `paste-selection` is blocked when disabled, `preferences` is handled when enabled, and the File menu `open-workspace` command is enabled and handled through a real menu click. A separate System Events check clicked the real macOS `Structra -> 偏好设置` menu item and produced both Rust-side `native-menu-event` and frontend-side `native-menu-command handled=true` audit records. The smoke then opens copied `.structra` fixtures through LaunchServices/startup args and waits for Tauri-written `open-document` audit records emitted only after the frontend reads and parses the external file path; startup args are retried once when macOS automation drops the open event. The same smoke drives the real macOS Open panel from the File menu through Accessibility sheet controls, selects a copied `.structra` fixture, and verifies an `open-document` audit with source `打开本机文档`. It also selects all nodes through the native Edit menu, verifies copy/delete/fit enabled states, copies and pastes through the native menu to prove paste transitions, saves the now-dirty document through the native menu, then relaunches the packaged app and verifies Recent ordering. Finally, it drives real Save As, Export JSON, Export SVG, Export PNG, current-page Export PDF, and document Export PDF save panels to fresh temp directories, verifies `save-document` and `export-document` audit records, checks the written files exist, parses the exported JSON and SVG, and verifies PNG/PDF file signatures. macOS did not expose a readable window title to AppleScript in this environment, so the title read is treated as auxiliary evidence rather than the pass/fail signal.

## Accessibility And Keyboard Pass

`docs/qa/accessibility-keyboard.md` and `scripts/qa/accessibility-keyboard-audit.mjs` now provide a repeatable local-only accessibility/keyboard audit path. The static helper is intentionally non-blocking but gives a quick signal while product functionality is being filled in. This pass added accessible names for the canvas editing region/React Flow surface, `role="tab"` plus `aria-selected` to local workspace template category controls, a command-palette active option model with ArrowUp/ArrowDown, Home/End, `aria-activedescendant`, disabled-item skipping, wraparound navigation, and visible active styling, context-menu `menu` / `menuitem` semantics with roving focus, shared modal focus lifecycle for command palette/preferences/presentation, current-state semantics for page/outline/layer rows, specific comment marker labels, a keyboard connector workflow, and a polite live status region for document state changes. The latest static audit reports `Passed: 14` and `Gaps: 0`.

## Commands

- `pnpm test`: passed, including the rich `.structra` round-trip fixture for pages, settings, comments, semantic fields, hidden-layer filtering, visible JSON/Mermaid/SVG export behavior, `.structra` native-name handling and fixture parsing, Tauri `.structra` file association configuration, native open-document event payload normalization/deduplication, desktop lifecycle audit hook exposure for open/save/export, explicit Mermaid/JSON failure boundaries, SVG XML attribute escaping, unsaved-change discard confirmation, structured Mind/Org child/sibling insertion, workspace route state, command registry coverage for all shape commands/default export/document PDF/print/match-size commands, local workspace action workflow exposure, local workspace favorite filtering/counting, and template favorite normalization/toggle/remove/persistence helpers.
- `cargo test --manifest-path src-tauri/Cargo.toml`: passed, including local file path validation for absolute paths and supported read/write extensions, now including `.structra`, external open-document argument/file-URL filtering, desktop lifecycle audit temp-path gating, native menu state audit writing through the same gated path, native menu audit command parsing, plus actual native command round-trip coverage that proves untrusted writes fail until a path is authorized, then writes/reads a temporary `.structra` document and writes a temporary `.png` export.
- `pnpm build`: passed.
- `pnpm test:e2e`: passed through `pnpm qa:smoke` and Playwright smoke automation, including standalone workspace rendering with no editor topbar/command toolbar/canvas behind it, workspace document identity display, workspace template favorite click, workspace "收藏" filter, favorite persistence across reload, editor sidebar favorite remove/re-add, recent-document format badges, non-destructive browser-preview recent open that stays in the workspace on failure, dismissible workspace-layer import errors, clear-list behavior, workspace/editor URL state across editor entry, visible topbar return-to-workspace, blank workspace entry, template workspace entry, and refresh, `.structra` document fixture imports through the browser file chooser, context-menu keyboard creation through `menu` roles, keyboard connector creation through the command palette plus Enter, toolbar and command-palette discovery for Mind/Org layout and structured child/sibling commands, layout-result undo/redo assertions for Mind/Org templates, Mind child/sibling insertion, Mind `Tab` child insertion, Mind `Enter` sibling insertion, Org child insertion, single-node drag move with undo/redo exported-coordinate assertions, node resize with undo/redo exported-size assertions, manual edge waypoint add and edge waypoint drag with undo/redo exported-route assertions, source and target reconnect with undo/redo exported-endpoint assertions, group move with undo/redo exported-coordinate assertions for both grouped nodes, and a local version save/restore path after an extra graph edit.
- `node scripts/qa/accessibility-keyboard-audit.mjs`: passed as a non-blocking static audit signal with 14 passing checks and 0 remaining static gaps.
- `pnpm qa:perf`: not rerun in this file-lifecycle pass by product direction; the latest recorded baseline below remains the current historical guardrail, not a fresh result from this pass.
- `cargo test` in `src-tauri`: latest package-scoped run passed through `cargo test --manifest-path src-tauri/Cargo.toml`.
- `pnpm tauri:build`: passed and produced `src-tauri/target/release/bundle/macos/Structra.app`; the production Tauri CSP is now non-null and verified by unit coverage plus the real packaging path. The latest package includes the frontend desktop lifecycle audit bridge for Open/Recent/Save/Save As/Export success paths.
- `pnpm qa:desktop`: passed the macOS desktop smoke for the freshly rebuilt `Structra.app`, including packaged `.structra` document type verification, launch/quit, native menu enabled-state audit for initial `document-save=false`, native-menu frontend dispatch evidence for disabled paste, enabled preferences, and enabled `open-workspace`, LaunchServices/open-args external open of copied `.structra` fixtures with frontend audit records after parsing, real native Open panel selection of a copied `.structra` fixture, real native Edit menu clicks for Select All, Copy, and Paste with copy/delete/fit/paste state transitions, real native Save Document against an opened `.structra` fixture, dirty-state transition evidence from `document-save=false` to `true` and back to `false`, a Tauri `save-document` audit for the active file path, relaunch audit proving Recent ordering after app restart, audit-only workspace Recent reopen with `workspace-recent-open-attempt` / `workspace-recent-open-result`, audit-only unsaved discard prompt evidence with `allowed=true` and `source=audit-override`, real Save As panel writing a `.structra` copy, real Export JSON panel writing and parsing `structra-diagram.json`, real Export SVG panel writing and parsing an SVG document, real Export PNG panel writing a PNG with a valid signature, and real current-page/document PDF panels writing PDF files with valid headers and EOF markers.

Latest focused coverage additions: `pnpm test` now asserts locked-node mutation protection for node property patches, batch style patches, labels, format painter, duplicate, group/ungroup, align/distribute, generic auto-layout, Mind/Org family-specific auto-layout, structured Mind/Org child/sibling insertion, `Tab`/`Enter` structure shortcuts, z-order, and layer movement; it also asserts document-level command behavior for inline node/edge labels, active-page graph replacement, active-page snapshot replacement, and whole-document replacement including active-page repair, settings normalization, comment normalization, selection, no-op rejection, and history undo/redo. Restore workflow tests now verify that restored versions prepare a whole-document baseline history entry. It also asserts the local import/export format-support matrix labels native, best-effort, and unsupported boundaries including `.structra`, the local workspace template filtering/summary model, local document identity and recent-file format display models, explicit file-lifecycle failure states for browser fallback, native write failure, native read failure, missing recent files, pure lifecycle patches for saved/opened-native/browser-download/new/imported/missing-recent states, unsaved-change discard confirmation, recent-list clearing, native menu enabled-state guards for undo/redo/save/selection/paste/preferences/snap, keyboard shortcut Enter dispatch for keyboard connector completion, pure keyboard connector source/target construction, local app preference normalization/persistence, canvas settings workflow exposure, document history controller exposure, local workspace action workflow exposure, shape insertion workflow exposure, non-history focus selection preparation and viewport intents, dedicated local mind map / org chart shape families, and at least three realistic templates for each P0 family: flowchart, BPMN, UML, ER, mind map, and org chart. It now also loads `tests/fixtures/roundtrip-rich-document.structra` and `tests/fixtures/smoke-import-document.structra` to prove native document parse/serialize/reparse behavior for multi-page documents, canvas settings, BPMN task semantics, ER PK/FK fields, table cells, comment replies, mind-map semantic fields, hidden nodes/edges, native extension handling, and visible JSON/Mermaid/SVG export filtering. Additional fixtures cover unsupported Mermaid sequence diagrams and corrupt flowchart node syntax. Playwright smoke now verifies that direct command-helper imports cover page/template, label, active-page graph, and active-page snapshot/comment transactions, that the document inspector exposes the matrix including Visio/VSDX as unsupported, that the app starts with the local workspace and document identity display, validates recent-document format badges, preserves workspace state after failed browser-preview recent open, clears local recent state, imports `.structra` document fixtures through file chooser, and returns to the editor, that context-menu keyboard navigation adds a decision node and undoes it, that keyboard connector authoring creates a right-source to left-target edge and undoes it, that the Mind/Org layout and structured child/sibling commands are visible in the toolbar or command palette and actually mutate template graphs with undo restoring them, that `Tab` and `Enter` grow Mind structures through the same undoable path, that single-node drag move changes exported document coordinates with undo/redo restoring them, that node resize changes exported node size with undo/redo restoring it, that manual edge waypoint add and waypoint drag change exported route data with undo/redo restoring them, that source and target reconnect change the exported edge endpoints/handles with undo/redo restoring them, that group move changes all grouped node coordinates with undo/redo restoring them, and that a saved local version can restore the graph after a later edit. The current App wiring also routes React Flow drag/resize end commits, pointer connect/reconnect commits, keyboard connector commits, edge waypoint/bend commits, comment add/reply/resolve/delete, document baseline replacement, page navigation baseline replacement, Mermaid import, version restore, Mind/Org auto-layout, structured Mind/Org insertion, local workspace entry actions, and non-history focus selection through extracted command/controller helpers. The Tauri file commands now reject relative paths, reject unsupported extensions, and require prior authorization before read/write. Native dialogs authorize selected paths, OS-opened paths are authorized before they are exposed through the pending-open bridge, and explicit Recent user actions authorize the selected local path before read. Command-level temp-file coverage proves unauthorized writes fail first, then authorized `.structra` document and `.png` export paths round trip. Document/text reads are limited to `.structra`, `.structra`, `.json`, `.mmd`, `.md`, and `.txt`; text writes are limited to `.structra`, `.structra`, `.json`, `.svg`, `.mmd`, `.md`, and `.txt`; binary writes are limited to `.png` and `.pdf`.

## Performance Baseline

The current baseline covers synthetic JSON/SVG work plus browser-driven interactive editing for 100, 500, and 1000 node fixtures. The interactive thresholds are current guardrails, not product-quality targets: 1000-node pan and zoom are visibly slow and remain P0 optimization risks.

| Nodes | Edges | JSON bytes | SVG bytes | JSON stringify median | JSON parse median | SVG export median |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 100 | 99 | 66609 | 107149 | 0.130 ms | 0.257 ms | 0.958 ms |
| 500 | 499 | 335932 | 544226 | 0.635 ms | 1.225 ms | 5.184 ms |
| 1000 | 999 | 672985 | 1093482 | 1.292 ms | 2.416 ms | 8.857 ms |

Interactive baseline from `output/playwright/interactive-performance-report.log`, viewport 1280x720. Render-ready, selection, zoom, pan, undo, and redo use 2 measured runs plus 1 warmup; drag move and area selection are one measured run with no warmup to keep `pnpm qa:perf` usable as a regression guardrail.

| Nodes | Edges | Render ready | Selection | Zoom | Pan | Drag move | Area selection | Undo | Redo |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 100 | 99 | 578 ms | 605 ms | 753 ms | 2564 ms | 4469 ms | 3186 ms | 569 ms | 522 ms |
| 500 | 499 | 1650 ms | 1689 ms | 2695.5 ms | 7562 ms | 10408 ms | 7132 ms | 1648 ms | 1722 ms |
| 1000 | 999 | 2782 ms | 3702 ms | 5327 ms | 14414.5 ms | 20364 ms | 13612 ms | 3612.5 ms | 3670 ms |

## Remaining Verification Gaps

- Native file dialogs now have desktop-level automation for real Open, Save As, Export JSON, Export SVG, Export PNG, current-page Export PDF, and document Export PDF picker paths, in addition to active-path native-menu Save. The current desktop smoke covers packaged file association, launch/quit, LaunchServices/open-args external open through the frontend native-open lifecycle, native menu dispatch, native workspace return, selection/copy/paste menu state transitions, active-path native-menu Save, native Open panel selection, Save As panel write, Export JSON panel write/parse, SVG XML parse checks, PNG signature checks, PDF header/EOF checks, Recent ordering after app restart, workspace Recent reopen after restart, and unsaved discard prompt audit via audit-only native commands. Remaining file-lifecycle evidence gaps are Finder double-click visual/window-title evidence and pixel/visual fidelity baselines for SVG/PNG/PDF exports. Fast unit coverage now exists for Tauri native file associations, external open-document event filtering/listening, desktop lifecycle audit gating, path authorization before file command access, open/save/export audit hook exposure, explicit failed read, failed write, native-unavailable fallback, missing recent-file status mapping, new dirty draft state, and local recent-list clearing.
- Native menu disabled states now have unit coverage, frontend dispatch protection, a packaged-app desktop audit proving the initial Save command is synchronized disabled when the document is not dirty, a packaged-app smoke assertion that disabled paste is blocked and enabled preferences/workspace commands are handled, real System Events menu-click audit for the preferences item, packaged-app selection/copy/paste state transitions through the native Edit menu, and a packaged-app dirty/save transition proving native paste enables Save and native Save clears it for an active `.structra` path. Remaining menu evidence should cover rendered OS menu inspection where automation permissions allow it.
- Performance evidence now covers render-ready, outline-driven selection, zoom, pan, single-node drag move, area selection, undo, and redo. The new drag/area metrics make the large-graph risk sharper: 500/1000-node drag move and area selection are regression-guarded but not product-quality interactions, and pan/zoom still need follow-up optimization.
- Accessibility coverage now has a static audit helper at 14/0 plus browser smoke for context-menu keyboard creation and keyboard connector creation. It still needs a dedicated browser-level accessibility smoke for focus trap/return focus, active list states, comment marker labels, toolbars, inspectors, page list, inline editors, live-region announcements, and selected-object text alternatives.
- Product-depth evidence is still incomplete for ProcessOn-like local authoring: each P0 diagram family now has at least three realistic built-in templates, Mind/Org have layout commands, child/sibling structure-growth commands, layout-result smoke, structured-insertion smoke, plus single and batch semantic inspector controls. Local version restore has a browser authoring-loop assertion, and the first rich `.structra` round-trip fixture covers semantic fields/comments/settings/hidden export filtering. The app still needs deeper family-specific layout options, pixel/visual export baselines, and a native desktop end-to-end loop from real save/open/export/recovery.
- Product-level gaps identified by the 2026-06-08 review, after the latest connector, CSP, workspace route, visible editor return button, desktop lifecycle, path-authorization, native-menu, native-picker, workspace Recent reopen, and unsaved-prompt audit passes: broader multi-step connector editing coverage, deeper Mind/Org layout options and family-specific authoring evidence, stricter 500/1000-node interaction targets, actual large-graph interaction optimization, Finder/window-title evidence where macOS automation allows it, and SVG/PNG/PDF pixel/visual export baselines.
